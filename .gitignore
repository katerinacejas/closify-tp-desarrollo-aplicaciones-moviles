# ── Build ─────────────────────────────────────────────────────────────────────
*.iml
.gradle
/build
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
*.ap_

# ── Configuración local (no versionar) ────────────────────────────────────────
local.properties

# ── Firebase (contiene API keys, no versionar) ────────────────────────────────
google-services.json

# ── Keystore y firma (NUNCA versionar) ────────────────────────────────────────
*.jks
*.keystore
signing.properties
release.properties

# ── Android Studio / IntelliJ ─────────────────────────────────────────────────
/.idea/

# ── Kotlin ────────────────────────────────────────────────────────────────────
.kotlin/

# ── macOS ─────────────────────────────────────────────────────────────────────
.DS_Store

# ── Claude Code ───────────────────────────────────────────────────────────────
.claude/

# ── Pre-commit (configuración local, no versionar) ────────────────────────────
.pre-commit-config.yaml

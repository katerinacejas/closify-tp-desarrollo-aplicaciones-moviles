package com.closify.myapplication.ui.theme

import androidx.compose.ui.graphics.Color

// ── Paleta Closify ────────────────────────────────────────────────────────────

// Primarios
val LilaPrimary  = Color(0xFFB9A7F7) // Lila pastel — primary light / botones dark
val PrimaryDark  = Color(0xFF7C3AED) // Violeta fuerte — botones principales light

// Secundarios y acentos
val RosaSecondary  = Color(0xFFF8BBD0) // Rosa pastel — botón secundario
val LavandaAccent  = Color(0xFFD8B4FE) // Lavanda suave — chips/filtros seleccionados

// Fondos y superficies
val BackgroundColor    = Color(0xFFFFF7FB) // Fondo rosado casi blanco
val SurfaceColor       = Color(0xFFFFFFFF) // Cards y contenedores
val SurfaceVariantColor = Color(0xFFF4ECFF) // Cards suaves / secciones

// Texto
val TextPrimary   = Color(0xFF2E2438) // Texto principal
val TextSecondary = Color(0xFF7B6B8F) // Texto secundario

// Semánticos
val SuccessColor = Color(0xFF8FD3C7) // Confirmaciones
val WarningColor = Color(0xFFFFD6A5) // Avisos suaves
val ErrorColor   = Color(0xFFE57373) // Errores

// Dark mode
val DarkBackgroundColor = Color(0xFF17111F) // Fondo principal
val DarkSurfaceColor = Color(0xFF241A2E) // Cards / contenedores
val DarkSurfaceVariantColor = Color(0xFF30233D) // Cards suaves / secciones
val DarkOutlineColor = Color(0xFF4A3A5F) // Bordes sutiles

val DarkTextPrimary = Color(0xFFF8F1FF) // Texto principal
val DarkTextSecondary = Color(0xFFC9B8DD) // Texto secundario

val DarkPrimary = Color(0xFFC7B8FF) // Lila principal en dark
val DarkPrimaryStrong = Color(0xFF9B6DFF) // Botones principales / acciones
val DarkSecondary = Color(0xFFFFB3D0) // Rosa pastel visible en dark
val DarkAccent = Color(0xFFE2C4FF) // Lavanda acento

val DarkSuccessColor = Color(0xFF9BE0D3)
val DarkWarningColor = Color(0xFFFFD39A)
val DarkErrorColor = Color(0xFFFF8A8A)
